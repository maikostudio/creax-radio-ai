
export interface AdScript {
  title: string;
  text: string;
  sfx: string;
  tone: string;
}

export interface AdProject {
  category: string;
  location: string;
  vibe: string;
  briefing: string;
}

export interface Vibe {
  id: string;
  name: string;
  icon: string;
  musicUrl: string;
  color: string;
}
